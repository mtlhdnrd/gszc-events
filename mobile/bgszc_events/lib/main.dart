import 'package:flutter/material.dart';
import 'package:bgszc_events/app.dart'; // Importálja az app.dart-ot

void main() {
  // Biztosítja, hogy a Flutter engine inicializálva legyen
  WidgetsFlutterBinding.ensureInitialized();

  // Az alkalmazás futtatása
  runApp(MyApp());
}